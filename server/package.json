{
  "name": "utsuroi-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Utsuroi backend — the boundary in front of the AI providers. Deployed as its own Vercel project (Root Directory: server).",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@vercel/node": "^5.8.23",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.2",
    "openai": "^6.46.0"
  }
}
