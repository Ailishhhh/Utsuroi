# constants

App-wide constant values.

Examples:

- `theme.ts` — the locked brand design tokens (colors, typography). [added in M1.3]
- Other fixed config values that never change at runtime.

Rules:

- No logic here beyond simple typed data structures.
